<section data-ng-controller="ReportTypesController" data-ng-init="findOne()">
    <span>{{reportType.created | date:'medium'}}</span> /
    <span>{{reportType.user.name}}</span>
    <h2>{{reportType.type}} - {{reportType.title}}</h2>
    <div data-ng-if="hasAuthorization(reportType)">
        <a class="btn" href="/#!/reportTypes/{{reportType._id}}/edit">
            <i class="glyphicon glyphicon-edit"></i>
        </a>
        <a class="btn" data-ng-click="remove(report);">
            <i class="glyphicon glyphicon-trash"></i>
        </a>
    </div>
    <div>{{report.value}}</div>
</section>
