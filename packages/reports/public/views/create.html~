<section data-ng-controller="ReportsController">
    <form name="reportForm" class="form-horizontal col-md-6" role="form" data-ng-init="" data-ng-submit="create(reportForm.$valid)" novalidate>
        <div class="form-group" ng-class="{ 'has-error' : submitted && reportForm.type.$invalid }">
            <label mean-token="'report-type'" class="col-md-3 control-label">Type</label>
            <div class="col-md-9">
                <!--<input name="title" type="text" class="form-control" data-ng-model="title" id="title" placeholder="Title" required>-->
               <select class="form-control" name="type" id="type"  required >
                  <optgroup>
                   <option ng-click="setType('')">Select Type</option>
                   <option ng-repeat= 'report in rTypes' ng-value="{{report}}" ng-click='setType(report)'  >{{report}}</option>
                  </optgroup>
               </select >                
                <div ng-show="submitted && reportForm.title.$invalid" class="help-block">
                    <p ng-show="reportForm.title.$error.required">Title is required</p>
                </div>
            </div>
            </div>
            <div class="form-group"  ng-class="{ 'has-error' : submitted && reportForm.title.$invalid }">
            <label mean-token="'report-title'" class="col-md-3 control-label">Title</label>
            <div class="col-md-9">
               <select class="form-control" ng-disabled='!report.reportType.type' name="title" id="title"    required >
                 <optgroup>                                    
                  <option ng-click='setTitle('')'>Select Title</option>
                  <option ng-repeat=" rtitle in rtitles" ng-value="{{rtitle}}" ng-click="setTitle(rtitle)"  > {{rtitle}}</option>
                 </optgroup>
               </select>
            <div ng-show="submitted && reportForm.title.$invalid" class="help-block">
                    <p ng-show="reportForm.title.$error.required">Title is required</p>
                </div>
            </div>
        </div>
        <div class="form-group"  ng-class="{ 'has-error' : submitted && reportForm.value.$invalid }">
            <label mean-token="'create-content'" for="content" class="col-md-3 control-label">Value</label>
            <div class="col-md-9">
                <input name="value" ng-disabled='! (report.reportType.type && report.reportType.title)' type="number" class="form-control" ng-model="report.value"  id="value" placeholder="Value" required>
                 <div ng-show="submitted && reportForm.value.$invalid" class="help-block">
                    <p ng-show="reportForm.value.$error.required">Value is required</p>
                    <p ng-show="reportForm.value.$error.max||reportForm.value.$error.min"> The value range should be from {{report.from}} to {{report.to}} </p>
                </div>
            </div>
        </div>
        <!--
        <div class="form-group" ng-class="{ 'has-error' : submitted && reportForm.value.$invalid }">
            <label mean-token="'create-date'" for="content" class="col-md-3 control-label">Date</label>
            <div class="col-md-9">
                <input name="date" type="date" class="form-control" data-ng-model="date" id="date" ui-date required>
                 <div ng-show="submitted && reportForm.date.$invalid" class="help-block">
                    <p ng-show="reportForm.content.$error.required">Value is required</p>
                </div>
            </div>
        </div> -->
        <div class="form-group">
            <div class="col-md-offset-3 col-md-9">
                <button type="submit" class="btn btn-info">Submit</button>
            </div>
        </div>
    </form>
</section>
